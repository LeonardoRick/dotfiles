# gitlab don't allow us to have the same ssh for differnet account, so here we can have a configuration for each account
# when clonning from 'personal' remember to change git clone git@gitlab.com:... to git clone git@gitlab_2:...
# for better information see: https://stackoverflow.com/questions/23537881/fingerprint-has-already-been-taken-gitlab
Host           gitlab.com
HostName       gitlab.com
IdentityFile   ~/.ssh/id_ed25519
User           leoanrdorick


Host           gitlab_personal
HostName       gitlab.com
IdentityFile   ~/.ssh/id_rsa_personal
User           leonardorick


# Fig ssh integration. Keep at the bottom of this file.
Match all
  Include ~/.fig/ssh
